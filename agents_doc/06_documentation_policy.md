# Documentation Policy

- Agent guidance documents live in `agents_doc/` and are linked from `AGENTS.md`.
- Project plans and long-form strategy documents live in `plans/`.
- Do not create new root-level documentation files outside these folders.
